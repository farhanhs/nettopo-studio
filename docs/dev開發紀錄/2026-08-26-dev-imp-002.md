# 2026-08-26 DEV_IMP_002 開發紀錄

## Ticket

- DEV_IMP_002 — Strict Import File／Manifest／Preview Gate
- 狀態：READY_FOR_QA
- 依據：DSG_IMP_002、CSV-BUNDLE-V2-CONTRACT、QA_IMP_001 首輪退件

## 本次修正

- `app/lib/topology-transfer.ts`
  - 新增 `ImportFileDescriptor`、`BundleManifest`、`BundleEnvelope` 與 import size constants。
  - 新增 metadata-first file gate：單檔 10 MiB、批次 25 MiB、canonical duplicate、path spoof、ZIP unsupported、mixed source fail closed。
  - CSV Bundle v2 改成 strict manifest/envelope：固定五檔、canonical header、all-header-only 空 safe bundle info issue、mixed schemaVersion/sharing blocking。
  - v1 legacy 三檔仍保留，但 v2 envelope/header 一旦出現不得 fallback 到 v1。
  - `canApply` 改為同時遵守 explicit blocking issue 與 missing-info blocking。

- `app/page.tsx`
  - `prepareImport()` 先以 `File.name/File.size/File.type` 建立 descriptor；只有 metadata gate 通過才呼叫 `File.text()`。
  - `confirmImport()` 除 UI disabled 外，再次 fail closed 檢查 `canApply`、blocking issues、missing blocking、warning acknowledgement。

- `app/components/import/ImportIssueList.tsx`
  - 支援 info issue 顯示，用於 `EMPTY_V2_BUNDLE`。

- `tests/dev-imp-002-transfer-contract.test.mjs`
  - 新增 DEV regression：limit-1/limit/limit+1、batch limit、all-header-only、v1/v2 truth table、duplicate canonical、path spoof、unknown member、mixed schemaVersion/sharing。

## 已執行驗證

- `node --test tests\dev-imp-002-transfer-contract.test.mjs tests\qa-imp-001-transfer-contract.test.mjs tests\topology-transfer.test.mjs`：PASS，27/27。
- `node node_modules\typescript\bin\tsc --noEmit --pretty false`：PASS。
- `node --test tests\*.test.mjs`：178 pass／9 skip／2 fail；兩個 fail 皆為真 PostgreSQL QA fixture 缺 `DATABASE_URL`（QA_DBM_001、QA_PIL_001），非本票 transfer regression。
- `node node_modules\typescript\bin\tsc --noEmit --pretty false`：PASS。
- scoped ESLint（本輪 transfer/page/import component/tests）：PASS。
- `npm.cmd run build:local`：PASS；僅既有 chunk size／route classification warning。
- `git diff --check`：PASS；僅 CRLF 提醒。

## 剩餘風險／下一步

- DEV_EXP_002 已由 BLOCKED 解為 READY，下一步處理 canonical export／round-trip equality。
- DEV_SEC_002 仍待 DEV_EXP_002 READY_FOR_QA 後處理 import/export secret boundary defense-in-depth。
- QA_IMP_001 必須保留首輪 QA_FAILED 歷史，等三張 DEV 修正票皆 READY_FOR_QA 後再交測試組重驗。

## QA Return Fix — 2026-08-29

### 背景

`QA_IMP_001` 2026-08-26 re-test 退回同票：strict parser／manifest／file gate 已通過，但 Browser Preview 仍有 duplicated controls。正常 Preview 看到 2 個 strategy select；oversized blocking issue 顯示 2 份。根因是 `app/page.tsx` 已掛入 `ImportPreviewModal`，後方仍保留 legacy picker／summary／issues／options／actions JSX，僅靠 CSS 隱藏部分節點。

### 修正

- `app/page.tsx`
  - 保留單一 `FileDropZone` 與單一 `ImportPreviewModal`。
  - 刪除後方 legacy file picker、summary、issues、strategy/name、replace warning、cancel/confirm JSX。
  - 保留 `confirmImport()` fail-closed re-check。

- `app/globals.css`
  - 移除只為遮蔽 legacy JSX 的 `.transfer-panel > ... { display: none; }` selectors。
  - 清理同一 import-preview 區塊的重複 CSS 宣告，不做視覺重設計。

- `tests/dev-imp-002-preview-ownership.test.mjs`
  - 新增 product regression，確認 page import block 不再擁有 legacy summary／issue／strategy／confirm controls。
  - 確認 `ImportPreviewModal` 只擁有 1 個 strategy select、1 個 confirm action、1 個 summary 與 1 個 issue list。
  - 確認 CSS 不再以 legacy hide selectors 作為 workaround。

### 驗證

- `node --test tests\dev-imp-002-preview-ownership.test.mjs tests\dev-imp-002-transfer-contract.test.mjs tests\qa-imp-001-transfer-contract.test.mjs tests\topology-transfer.test.mjs`：PASS，30/30。
- `npm.cmd run db:local:start`：PASS；PostgreSQL accepting connections on `127.0.0.1:5432`。
- `npm.cmd run db:verify`：PASS；schema ready，required/current version `0004`。
- `node --env-file-if-exists=.env.local --env-file-if-exists=.env tests\qa-imp-001-browser-retest.mjs`：PASS。Browser summary 顯示 `strategySelectCount=1`、`confirmButtonCount=1`、`duplicateIssueMessageCount=1`、oversized `File.text()` spy 未被呼叫、DB cleanup 全部 0。此為 developer preflight，不作為 QA 通過聲明。
- `node node_modules\typescript\bin\tsc --noEmit --pretty false`：PASS。
- scoped ESLint：PASS；`app/globals.css` 被 config 忽略，顯示 ignored warning、exit 0。
- `npm.cmd run build:local`：PASS；僅既有 plugin timing／chunk size／route classification warning。
- `git diff --check`：PASS；僅 CRLF 提醒。

### 狀態

- DEV_IMP_002：READY_FOR_QA。
- QA_IMP_001 仍須由測試組同票 re-test；開發組未啟動 QA／Release／checkpoint／deploy。
