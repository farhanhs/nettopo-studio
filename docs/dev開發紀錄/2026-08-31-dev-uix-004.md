# DEV_UIX_004 開發紀錄 — 登入者資料卡不得遮擋 Inspector

日期：2026-08-31
狀態：READY_FOR_QA

## 變更摘要

- 將 `app/page.tsx` 的 `session-profile` 從頁面底部 fixed overlay 移入 topbar 的 `save-state` 正常 layout flow。
- 保留登入者姓名、角色、email、`aria-label="登入者資料"` 與登出按鈕。
- 調整 `app/globals.css`，移除 `.session-profile` 的 `position: fixed`、`right`、`bottom`、`z-index`、overlay shadow/backdrop，改為 topbar 內 compact profile card。
- 新增 `tests/dev-uix-004-session-profile-layout.test.mjs`，防止 session profile 回到 fixed bottom-right overlay，並確認其在 topbar flow 內。

## 驗證結果

- `node --test tests/dev-uix-004-session-profile-layout.test.mjs`：PASS，2/2。
- `node node_modules\typescript\bin\tsc --noEmit --pretty false`：PASS。
- `node_modules\.bin\eslint.cmd app\page.tsx app\globals.css tests\dev-uix-004-session-profile-layout.test.mjs`：PASS，0 errors；`app/globals.css` 因 ESLint 設定未套用 CSS 顯示 1 warning。
- `npm.cmd run db:verify`：PASS，schema ready，required/current version `0004`。
- `node --check tests\qa-pil-003-engineer-uat.mjs`：PASS。
- `npm.cmd run build:local`：PASS，僅既有 chunk size / route classification warning。

## Browser Preflight

開發組未修改 `tests/qa-pil-003-engineer-uat.mjs`，直接以真 Browser Pilot UAT 作 preflight：

```powershell
node --env-file-if-exists=.env.local --env-file-if-exists=.env tests\qa-pil-003-engineer-uat.mjs
```

結果：preflight 已越過原本 `aside.session-profile intercepts pointer events` 的失敗點。`儲存連線` 使用正常 pointer click 觸發 `/api/topology` POST，response status `200`。

後續完整 UAT 停在新的斷言：

- failure：`wired routes should be rendered`
- evidence canvas：`wiredCount=1`、`wirelessCount=1`、`orthogonalPaths=1`
- cleanup：sites/users/customers/topologies/credentials/audit_logs 全部 `0`

此後段 routing count failure 不是本票的 fixed session card blocker；需由 QA_PIL_003 re-test 判定是否另開最小修復票。`儲存設備` 的真 Browser 正常 click 也交由 QA_PIL_003 快速重驗確認。

## 影響範圍

- 未修改 auth/session/RBAC/API/DB/schema/migration/import/export/routing。
- 未使用 force click、JS click、`pointer-events:none`、提高 Inspector z-index 或修改 QA script 掩蓋問題。
- 未 push、未 deploy。
