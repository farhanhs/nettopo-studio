# 2026-08-26 DEV_SEC_002 開發紀錄

## Ticket

- DEV_SEC_002 — Import／Export Secret Boundary Hardening
- 狀態：READY_FOR_QA
- 依據：CSV-BUNDLE-V2-CONTRACT、DEV_IMP_002 READY_FOR_QA、DEV_EXP_002 READY_FOR_QA

## 本次修正

- `app/lib/topology-transfer.ts`
  - `materializeImportBundle()` 在 Apply 前重新執行 `canonicalProjectForTransfer(plan.project)`。
  - 即使外部 forged `canApply=true` plan，也只能 materialize sanitized Project。

- `tests/dev-sec-002-secret-boundary.test.mjs`
  - JSON／CSV／ZIP export raw secret sentinel regression。
  - masked credential 僅留在 preview/export row，不進 materialized Project。
  - forged plan Apply defense-in-depth strip。
  - legacy v1 username/password 只生成 masked preview。

## 已執行驗證

- `node --test tests\dev-sec-002-secret-boundary.test.mjs tests\dev-imp-002-transfer-contract.test.mjs tests\dev-exp-002-export-contract.test.mjs tests\qa-imp-001-transfer-contract.test.mjs tests\topology-transfer.test.mjs tests\topology-validation.test.mjs`：PASS，41/41。
- `node --test tests\*.test.mjs`：178 pass／9 skip／2 fail；兩個 fail 皆為真 PostgreSQL QA fixture 缺 `DATABASE_URL`（QA_DBM_001、QA_PIL_001），非本票 transfer regression。
- `node node_modules\typescript\bin\tsc --noEmit --pretty false`：PASS。
- scoped ESLint（本輪 transfer/page/import component/tests）：PASS。
- `npm.cmd run build:local`：PASS；僅既有 chunk size／route classification warning。
- `git diff --check`：PASS；僅 CRLF 提醒。

## 剩餘風險／下一步

- Browser／Dexie／network／audit secret scan 不由 DEV 自證，需交 QA_IMP_001 re-test 獨立驗證。
- 三張 DEV 修正票皆已 READY_FOR_QA，可交測試組執行 QA_IMP_001 re-test，並保留首輪 QA_FAILED 歷史。
