# 2026-08-26 DEV_EXP_002 開發紀錄

## Ticket

- DEV_EXP_002 — Canonical Five-file Export／Round-trip Correction
- 狀態：READY_FOR_QA
- 依據：CSV-BUNDLE-V2-CONTRACT、DEV_IMP_002 READY_FOR_QA

## 本次修正

- `app/lib/topology-transfer.ts`
  - 新增 `canonicalProjectForTransfer()`，作為 Full／Safe round-trip regression 的 canonical 比較基準。
  - CSV formula neutralization 擴充至 LF 與全形 `＝＋－＠`。

- `tests/dev-exp-002-export-contract.test.mjs`
  - 覆蓋 canonical five-file export、ZIP 解壓五檔、Full/Safe round-trip equality、schemaVersion/sharing consistency、formula hardening。

## 已執行驗證

- `node --test tests\dev-imp-002-transfer-contract.test.mjs tests\dev-exp-002-export-contract.test.mjs tests\qa-imp-001-transfer-contract.test.mjs tests\topology-transfer.test.mjs`：PASS，33/33。
- `node --test tests\*.test.mjs`：178 pass／9 skip／2 fail；兩個 fail 皆為真 PostgreSQL QA fixture 缺 `DATABASE_URL`（QA_DBM_001、QA_PIL_001），非本票 transfer regression。
- `node node_modules\typescript\bin\tsc --noEmit --pretty false`：PASS。
- scoped ESLint（本輪 transfer/page/import component/tests）：PASS。
- `npm.cmd run build:local`：PASS；僅既有 chunk size／route classification warning。
- `git diff --check`：PASS；僅 CRLF 提醒。

## 剩餘風險／下一步

- DEV_SEC_002 已由 BLOCKED 解為 READY，下一步補 import/export secret boundary regression。
- 真 Browser／Dexie／network／audit secret scan 仍交由 QA_IMP_001 re-test 驗證。
