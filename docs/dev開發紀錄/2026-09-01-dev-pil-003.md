# DEV_PIL_003 開發紀錄 — 2026-09-01

## 結果

Workspace 登出依 runtime profile 只呼叫一個 session endpoint：Pilot 使用 `/api/pilot/session`，development/demo 使用 `/api/dev/session`。不再跨 profile 串行嘗試兩個登出 API。

## 驗證

- DEV_PIL_003 targeted tests：2/2 PASS。
- UIX/PIL 合併 targeted tests：8/8 PASS。
- TypeScript、scoped ESLint、Pilot build：PASS。
- QA_PIL_003 獨立 UAT：實際 DELETE pathname 為 `/api/pilot/session`、response 200；登出後 topology 401，tampered cookie 401。

## 邊界

- Cookie options、Pilot binding、same-origin、RBAC 與 API handler 語意未變更。
- Runtime capabilities 未載入時不跨 profile 嘗試多個 endpoint。
