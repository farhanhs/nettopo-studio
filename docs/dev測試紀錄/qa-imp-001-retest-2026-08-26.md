# QA_IMP_001 Re-test — Import／Export Browser E2E、Round-trip 與 Secret Scan

日期：2026-08-26  
結果：`QA_FAILED`

本紀錄為同票 Re-test；保留 `docs/dev測試紀錄/qa-imp-001-2026-08-25.md` 首輪 `QA_FAILED` 歷史，不覆寫原始失敗證據。

## Scope

本輪依 `DSG_IMP_002`、`CSV-BUNDLE-V2-CONTRACT`、`DEV_IMP_002`、`DEV_EXP_002`、`DEV_SEC_002` 交付內容複驗：

- Strict import contract：五檔完整性、duplicate canonical/path spoof、unknown member、mixed sharing/version、all-header-only v2 safe bundle、v1/v2 truth table。
- Browser file gate：oversized input 必須在 `File.text()`／Papa Parse／Zod 前 fail closed。
- Browser Preview：不得同時出現兩套 strategy/confirm controls。
- Export／round-trip：canonical five-file ZIP、Full／Safe canonical equality、formula neutralization。
- Secret boundary：masked credentials preview-only；raw secret/ciphertext/nonce 不進 Project/export。

## 新增／使用的 QA-only 證據

- `tests/qa-imp-001-browser-retest.mjs`
- `docs/dev測試紀錄/qa-imp-001-browser-retest-summary.json`
- `docs/dev測試紀錄/screenshots/qa-imp-001-retest-oversized-file-gate.png`
- `docs/dev測試紀錄/screenshots/qa-imp-001-retest-preview-controls.png`

## Command Results

| 指令 | 結果 | 摘要 |
|---|---:|---|
| `node --test tests\dev-sec-002-secret-boundary.test.mjs tests\dev-imp-002-transfer-contract.test.mjs tests\dev-exp-002-export-contract.test.mjs tests\qa-imp-001-transfer-contract.test.mjs tests\topology-transfer.test.mjs tests\topology-validation.test.mjs` | PASS | 41/41；首輪四個 QA_FAILED probes 已通過 |
| `npm.cmd run db:local:status` | PASS | PostgreSQL initialized but stopped |
| `npm.cmd run db:local:start` | PASS | 既有 cluster 啟動；accepting connections on 127.0.0.1:5432 |
| `npm.cmd run db:verify` | PASS | schema ready，required/current version 0004 |
| `node --env-file-if-exists=.env.local --env-file-if-exists=.env tests\qa-imp-001-browser-retest.mjs` | FAIL | Browser Preview duplicate strategy control：expected 1, found 2 |
| `node node_modules\typescript\bin\tsc --noEmit --pretty false` | PASS | 無 TypeScript error |
| scoped ESLint | PASS | 本輪 QA script、transfer tests、transfer/page/import components 0 error |
| `npm.cmd run build:local` | PASS | 只有既有 chunk size／route classification warning |
| `node --env-file-if-exists=.env.local --env-file-if-exists=.env --test tests\*.test.mjs` | PASS | 189/189 pass；真 PostgreSQL tests 也通過 |
| `git diff --check` | PASS | 僅 CRLF 提醒 |
| `npm.cmd run lint` | BLOCKED | Windows 環境缺 `bash`，既有 npm script blocker |

## Browser Evidence

Browser automation 使用 loopback Pilot preview 與 QA-only PostgreSQL fixture；不輸出 cookie value、password、完整 DSN、allowlist 原文或 credential plaintext。

### PASS — Pilot / protected API

- Pilot login：200。
- Post-login protected `/api/topology` probe：200。
- Workspace session profile visible：PASS。

### PASS — File Gate

Oversized CSV 使用 Browser `File` 物件並覆寫 `File.text()` spy。

- 預期：大於 10 MiB 的 CSV 必須在 `File.text()` 前 blocking。
- 實際：`File.text()` spy 未被呼叫。
- DB snapshot：oversized blocked preview 前後一致，zero-write。
- 截圖：`qa-imp-001-retest-oversized-file-gate.png`。

附帶觀察：同一 oversized error message 在 UI 中出現 2 份，`duplicateIssueMessageCount=2`。

### FAIL — Preview controls are duplicated

正常五檔 v2 CSV bundle 開啟 preview 後：

```json
{
  "strategySelectCount": 2,
  "confirmButtonCount": 1,
  "expectedStrategySelectCount": 1,
  "expectedConfirmButtonCount": 1
}
```

預期：同一 Import Preview modal 只能有一套 strategy／confirm controls。  
實際：strategy select 有 2 個，違反 `QA_IMP_001` IMP-B 與 `CSV-BUNDLE-V2-CONTRACT` section 9。  
截圖：`qa-imp-001-retest-preview-controls.png`。

## Fixture / Cleanup

QA-only fixture prefix：`qa_imp_001_retest`。

Cleanup result：

```json
{
  "sites": 0,
  "users": 0,
  "customers": 0,
  "topologies": 0,
  "credentials": 0,
  "audit_logs": 0
}
```

Preview server port `4395` 檢查：無 listener 殘留。

## Pass / Fail Matrix

| Area | Result | Notes |
|---|---:|---|
| IMP-A strict parser／manifest | PASS | 首輪四個 blockers 已修復 |
| File.text pre-read oversized gate | PASS | Browser spy + DB zero-write |
| v1 compatibility | PASS | Node regression covered |
| export five-file ZIP / canonical equality | PASS | Node regression covered |
| formula neutralization | PASS | ASCII、LF、全形 triggers covered |
| secret boundary developer regression | PASS | JSON／CSV／ZIP／materialize tests covered |
| real PostgreSQL regression | PASS | full Node tests 189/189 includes QA_DBM/QA_PIL real DB |
| Browser Preview unique controls | FAIL | strategy select count 2 |
| new/merge/replace Browser apply | NOT RUN | blocked by duplicated Preview controls |
| full Browser round-trip download/import/re-export | NOT RUN | blocked by duplicated Preview controls |
| complete DOM/network/DB/Audit sentinel scan | NOT RUN | blocked before apply/round-trip |

## Conclusion

`QA_IMP_001` Re-test 仍不通過，狀態維持 `QA_FAILED`。

開發組已修復首輪 IMP-A parser/manifest/file-gate failures，且 Node／DB regression 通過；但 Browser Preview 尚未符合合約，仍存在 duplicated strategy control 與 duplicated issue rendering。請退回 `DEV_IMP_002` 的 Preview/UI hunk 修正，移除舊 import summary/options/confirm controls 或確保它們不可操作、不可造成狀態繞過，再交 QA 同票 re-test。
