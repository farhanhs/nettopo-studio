# QA_IMP_001 Re-test — DEV_IMP_002 QA Return Fix

日期：2026-08-29  
結果：`QA_PASSED`

本紀錄為同票 Re-test；保留 `docs/dev測試紀錄/qa-imp-001-2026-08-25.md` 首輪 `QA_FAILED` 與 `docs/dev測試紀錄/qa-imp-001-retest-2026-08-26.md` 第二輪 `QA_FAILED` 歷史，不覆寫原始失敗證據。

## Scope

本輪依 `DEV_IMP_002` 2026-08-29 QA Return Fix 複驗：

- Browser Preview 不得再出現 duplicated strategy / issue / confirm controls。
- oversized file gate 必須在 `File.text()`／Papa Parse／Zod 前 fail closed。
- warning acknowledgement、blocking fail-closed、Cancel zero-write、new／merge／replace apply 不回歸。
- Safe CSV ZIP 五檔、解壓後重新匯入與 secret boundary 不回歸。
- strict parser／manifest、export round-trip、secret boundary、TypeScript、scoped ESLint、build:local、full Node tests 與 DB verify 不回歸。

## QA-only 測試與證據

- `tests/qa-imp-001-browser-retest.mjs`
  - 本輪在 QA-only script 追加 new／merge／replace、warning ack、blocking DOM-hack fail-closed、safe ZIP download／unzip／re-import round-trip 與 credential zero-write 檢查。
- `docs/dev測試紀錄/qa-imp-001-browser-retest-summary.json`
- `docs/dev測試紀錄/screenshots/qa-imp-001-retest-oversized-file-gate.png`
- `docs/dev測試紀錄/screenshots/qa-imp-001-retest-preview-controls.png`

## Command Results

| 指令 | 結果 | 摘要 |
|---|---:|---|
| `npm.cmd run db:local:status` | PASS | PostgreSQL ready at loopback。 |
| `npm.cmd run db:verify` | PASS | schema ready，required/current version `0004`。 |
| `node --test tests\dev-imp-002-preview-ownership.test.mjs tests\dev-imp-002-transfer-contract.test.mjs tests\qa-imp-001-transfer-contract.test.mjs tests\topology-transfer.test.mjs` | PASS | 30/30；Preview ownership 與 IMP-A strict contract 通過。 |
| `node --test tests\dev-sec-002-secret-boundary.test.mjs tests\dev-imp-002-transfer-contract.test.mjs tests\dev-exp-002-export-contract.test.mjs tests\qa-imp-001-transfer-contract.test.mjs tests\topology-transfer.test.mjs tests\topology-validation.test.mjs` | PASS | 41/41；transfer/export/sec targeted regression 通過。 |
| `node --env-file-if-exists=.env.local --env-file-if-exists=.env tests\qa-imp-001-browser-retest.mjs` | PASS | Browser re-test 通過；fixture cleanup 全部 0。 |
| `node node_modules\typescript\bin\tsc --noEmit --pretty false` | PASS | 無 TypeScript error。 |
| scoped ESLint | PASS | QA script、transfer tests、page、transfer core、Import components 0 error。 |
| `npm.cmd run build:local` | PASS | 僅既有 plugin timing／chunk size／route classification warnings。 |
| `node --env-file-if-exists=.env.local --env-file-if-exists=.env --test tests\*.test.mjs` | PASS | 192/192 pass；含真 PostgreSQL QA_DBM／QA_PIL integration。 |
| `git diff --check` | PASS | 僅 CRLF warning，無 whitespace error。 |
| `npm.cmd run lint` | BLOCKED | Windows 環境缺 `bash`，既有 npm script blocker；本輪以 scoped ESLint 補證。 |

## Browser Evidence Summary

Browser automation 使用 loopback Pilot preview 與 QA-only PostgreSQL fixture；不輸出 cookie value、password、完整 DSN、allowlist 原文或 credential plaintext。

| Area | Result | Evidence |
|---|---:|---|
| Pilot login / protected probe | PASS | login 200；post-login `/api/topology` probe 200。 |
| oversized file gate | PASS | `File.text()` spy 未被呼叫；DB zero-write；同一 blocking issue message count = 1。 |
| Preview unique controls | PASS | `strategySelectCount=1`；`confirmButtonCount=1`。 |
| warning acknowledgement | PASS | confirm 在 ack 前 disabled，ack 後 enabled；Cancel zero-write。 |
| blocking fail-closed | PASS | blocking preview confirm disabled；移除 DOM disabled 後點擊仍 DB zero-write。 |
| new apply | PASS | 新拓樸建立成功：2 devices／1 link／1 group。 |
| merge apply | PASS | 同 bundle 合併後 remap：4 devices／2 links／2 groups。 |
| replace apply | PASS | 目前拓樸取代為 1 device／0 links／1 group；sibling topology 未受影響。 |
| masked credentials | PASS | `credentials.masked.csv` 僅 preview；`device_credentials` QA prefix rows = 0。 |
| Safe CSV export | PASS | ZIP members exactly：`devices.csv`、`links.csv`、`groups.csv`、`credentials.masked.csv`、`missing-info.csv`。 |
| Safe Browser round-trip | PASS | 下載 ZIP 解壓五檔後重新匯入並 replace，維持 1 device／0 links／1 group。 |
| Full CSV gate | PASS | Pilot Engineer `完整 CSV` disabled。 |
| Secret scan | PASS | 解壓後 CSV 內容未命中 QA 禁止 sentinel；未輸出敏感值。 |

## Fixture / Cleanup

QA-only fixture prefix：`qa_imp_001_retest`。

Final cleanup：

```json
{
  "sites": 0,
  "users": 0,
  "customers": 0,
  "topologies": 0,
  "credentials": 0,
  "audit_logs": 0
}
```

Preview server port `4395` 檢查：無 listener 殘留。

## Pass / Fail Matrix

| Area | Result | Notes |
|---|---:|---|
| IMP-A strict parser／manifest | PASS | 首輪四個 blockers 維持修復。 |
| IMP-B Preview unique controls | PASS | 8/26 duplicated strategy blocker 已解除。 |
| warning acknowledgement | PASS | ack 前不可 apply，ack 後可 apply；Cancel zero-write。 |
| blocking fail-closed | PASS | disabled UI 與 handler-side zero-write 均驗證。 |
| IMP-C new／merge／replace | PASS | 真 Browser + PostgreSQL snapshots。 |
| EXP-A Safe ZIP five-file | PASS | 下載 ZIP 解壓五檔驗證。 |
| EXP-B Safe Browser round-trip | PASS | ZIP 解壓五檔重新匯入 apply/reload-equivalent DB verification。 |
| Full export gate | PASS | Pilot Engineer full CSV disabled；full canonical equality 由 targeted Node regression 覆蓋。 |
| SEC-A secret boundary | PASS | masked preview-only、credential table zero-write、ZIP secret scan pass。 |
| Real PostgreSQL regression | PASS | full Node tests 192/192 includes QA_DBM/QA_PIL real DB。 |
| Full lint | BLOCKED | 既有 Windows bash 缺失；scoped ESLint pass。 |

## Conclusion

`QA_IMP_001` 同票 Re-test 通過，狀態更新為 `QA_PASSED`。

8/26 的 Browser Preview duplicated strategy control blocker 已解除；本輪未發現需退回 `DEV_IMP_002`／`DEV_EXP_002`／`DEV_SEC_002` 的新 blocker。Release／checkpoint／deploy 仍須等待 PM 另行授權，本次 QA 不啟動後續票。
