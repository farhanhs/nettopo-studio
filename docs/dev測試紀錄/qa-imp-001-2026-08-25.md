# QA_IMP_001 — Import／Export Browser E2E、Round-trip 與 Secret Scan

日期：2026-08-25  
結果：`QA_FAILED`

## Requirement Traceability

| 欄位 | 內容 |
|---|---|
| User Intent | TXT／MD／CSV 預覽確認後才寫入；CSV Bundle v2 五檔可安全雙向交換並驗證還原 |
| Historical Sources | `QA_IMP_001.md`、`DEV_SEC_001.md`、`DEV_IMP_001.md`、`DEV_EXP_001.md`、`PM_GOV_001-D22` |
| Approved Decisions | D04／D07／D11／D12／D22 |
| Change Type | VERIFY；本次只新增 QA-only 測試與紀錄 |
| Affected Layers | Parser、Preview、Zustand、Dexie、API、PostgreSQL、ZIP、Security、Browser |
| Preserved Invariants | QA 不修改產品碼、DB role/grant、migration、OPS script 或 `.env.local` |

## 新增 QA-only 測試

- `tests/qa-imp-001-transfer-contract.test.mjs`

用途：把 `PM_GOV_001-D22` 與 `QA_IMP_001` 的 IMP-A／EXP-A 合約變成可重跑的自動化證據，尤其是 CSV Bundle v2 五檔完整性、重複檔名、sharing 一致性、size gate、duplicate ID、ZIP import reject 與 formula injection neutralization。

## Browser 工具狀態

- 已依本 session 提供的 Browser skill path 讀取並初始化 current in-app Browser runtime。
- in-app Browser client setup 成功，未判定為工具 blocker。
- 因 IMP-A 已出現「五檔契約錯誤」stop condition，未繼續建立 Pilot fixture 或執行會寫入 DB/Dexie 的 Browser apply/round-trip 流程。
- 無 screenshots 產出；沒有拍攝或輸出任何 cookie、token、DSN、allowlist 或 credential。

## 自動化測試結果

| 指令 | 結果 | 摘要 |
|---|---:|---|
| `node --test tests\qa-imp-001-transfer-contract.test.mjs` | FAIL | 7 tests：3 pass／4 fail |
| `node --test tests\topology-transfer.test.mjs tests\topology-validation.test.mjs tests\credential-crypto.test.mjs` | PASS | 19/19 pass；既有 transfer/validation/credential 護欄未回歸 |
| `node node_modules\typescript\bin\tsc --noEmit --pretty false` | PASS | 無 TypeScript error |
| `node_modules\.bin\eslint.cmd tests\qa-imp-001-transfer-contract.test.mjs tests\topology-transfer.test.mjs tests\topology-validation.test.mjs` | PASS | scoped ESLint 0 error |
| `npm.cmd run build:local` | PASS | 只有既有 chunk size／route classification warning |
| `git diff --check` | PASS | 僅 CRLF 提醒 |
| `npm.cmd run lint` | BLOCKED | Windows 環境仍缺 `bash`：`'bash' is not recognized...` |
| `node --env-file-if-exists=.env.local --env-file-if-exists=.env --test tests\*.test.mjs` | FAIL | 173 tests：156 pass／17 fail；包含本票 4 個合約失敗，另有真 PostgreSQL `127.0.0.1:5432` refused |

## Blockers

### IMP-A-001 — CSV Bundle v2 五檔不齊仍被接受

- 重現：由 `projectToCsvFiles()` 產出 v2 bundle 後刪除 `credentials.masked.csv`，呼叫 `buildImportPlan()`。
- 預期：v2 bundle 必須包含 `devices.csv`、`links.csv`、`groups.csv`、`credentials.masked.csv`、`missing-info.csv`；缺任一檔應 blocking。
- 實際：`plan.canApply === true`，`issues=[]`。
- 路由：`DEV_IMP_002`。

### IMP-A-002 — duplicate CSV filename 以 last-wins 被接受

- 重現：同批來源放入兩份 `devices.csv`。
- 預期：duplicate filename 必須 blocking，避免使用者看見與實際 parsed file 不一致。
- 實際：`plan.canApply === true`，`issues=[]`。
- 路由：`DEV_IMP_002`。

### IMP-A-003 — v2 bundle safe/full sharing 混用仍被接受

- 重現：同一 bundle 內 `devices.csv` 為 safe、`links.csv` 改為 full。
- 預期：五檔 sharing 必須一致，否則 blocking。
- 實際：`plan.canApply === true`，`issues=[]`。
- 路由：`DEV_IMP_002`／`DEV_EXP_002`。

### IMP-A-004 — 未找到 D22 10MiB single-file／25MiB batch 前置 size gate

- 重現：先前以 10MiB+ Markdown runtime probe 執行 `buildImportPlan()`，超過 60 秒仍未完成，已中止；後續以 QA-only gate 檢查確認匯入入口未見明確 10MiB／25MiB limit。
- 預期：oversized file/batch 應在 parser 前被拒絕並回 blocking issue。
- 實際：核心 `ImportSource` 只有 `name/text`，未保留 size metadata；UI/核心未見 D22 limit gate。
- 路由：`DEV_IMP_002`。

## 已通過的相關護欄

- Duplicate device ID：blocking。
- ZIP import：unsupported/rejected。
- CSV formula injection：`= + - @` 開頭字串已加前綴。
- 既有 safe/full JSON/CSV 明文 credential strip 與 masked credential 基礎測試通過。

## 未完成／未執行項目

因 `QA_IMP_001` stop condition 已被 IMP-A 五檔契約錯誤觸發，下列項目未繼續執行到會寫入資料的完整流程：

- 真 Browser drag/drop/select、Preview、Cancel zero-write、Apply。
- `new`／`merge`／`replace` PostgreSQL/Dexie snapshot。
- Full/Safe round-trip ZIP 下載、解壓、重新匯入、canonical equality。
- Project/Zustand/Dexie/DOM/console/network/DB/Audit/JSON/CSV/ZIP 完整 sentinel scan。

本次未建立 QA-only PostgreSQL 或 Dexie fixture，因此 cleanup count：0 created / 0 remaining。

## 結論

`QA_IMP_001` 不通過，狀態應為 `QA_FAILED`。建議退回：

- `DEV_IMP_002`：補 v2 五檔必備、duplicate filename、sharing consistency、size/batch limit 前置 gate。
- `DEV_EXP_002`：補 sharing canonical 與 round-trip 合約後再由 QA 重驗。
- `DEV_SEC_002`：待上述 import/export blocker 修正後，再執行完整 secret boundary integration。
